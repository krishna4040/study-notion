import React from 'react'

const page = () => {

    const handleBuyCourse = async () => {

    }

    return (
        <div>
            <button onClick={handleBuyCourse}>Buy Now</button>
        </div>
    )
}

export default page